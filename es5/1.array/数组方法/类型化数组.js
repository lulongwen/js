// Int16Array类型数组, 代表二进制补码, 16位有符号整数形式的 字节的数组
// 初始化内容为0。一旦数组建立，您可以使用数组的方法或使用标准数组索引（即使用括号表示法）引用数组中的元素。

var int16 = new Int16Array(5)

// 类型数组，表示一个二进制8位有符号整数数组
Int8Array()
Int16Array()

// 类型数组表示在平台字节顺序中的一对双补码32位有符号整数数组
Int32Array()


Uint8ClampedArray()


Uint8Array()
Uint16Array()
Uint32Array()

// 类型数组，代表的是平台字节顺序为32位的浮点数型数组(对应于 C 浮点数据类型) 
Float32Array()

// 类型数组，代表的是平台字节顺序为64位的浮点数型数组(对应于 C 浮点数据类型) 
Float64Array()